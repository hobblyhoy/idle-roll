import IRSection from '../../design-system/IRSection';

function Buffs() {
   return (
      <IRSection label="Buffs">
         <div>Nothing yet...</div>
      </IRSection>
   );
}

export default Buffs;
